# HeartWatch host app — Python 3.x
# Shell + Dashboard + Live Monitor (build steps 1–3)
PySide6>=6.6
matplotlib>=3.8

# Later build steps (kept here for reference; safe to install now):
#   step 4 — AI assistant bar
anthropic>=0.39
#   step 6 — BLE stream from XIAO ESP32-S3
bleak>=0.22
#   step 6 — on-device activity model
#   tflite-runtime  (platform-specific; install separately)
#   Plotly for offline/historical session charts
plotly>=5.20
